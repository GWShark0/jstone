import React from 'react';
import Modal from './Modal';

function MediaModal(props) {
  return (
    <Modal {...props}>
      <h2>
        {'Media modal'}
      </h2>
    </Modal>
  );
}

export default MediaModal;
