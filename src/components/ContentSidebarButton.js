import React from 'react';

import './ContentSidebarButton.scss';

function ContentSidebarButton(props) {
  return (
    <button className="content-sidebar-button" {...props} />
  );
}

export default ContentSidebarButton;
